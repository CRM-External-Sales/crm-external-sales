import { Button } from "@/components/ui/button";

export const View = () => {
  return (
    <div>
      <Button>Click</Button>
      <Button variant="destructive">Click red</Button>
    </div>
  );
};
